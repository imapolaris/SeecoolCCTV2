<Window x:Class="TestReadData.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:TestReadData"
        mc:Ignorable="d"
        Title="MainWindow" Height="350" Width="525">
    <Window.Resources>
        <Style TargetType="Button">
            <Setter Property="Width" Value="60"/>
            <Setter Property="Margin" Value="2,1"/>
        </Style>
    </Window.Resources>
    <DockPanel>
        <DockPanel DockPanel.Dock="Top" Height="30">
            <Label Content="Indexs地址：" />
            <Button Content="打开" DockPanel.Dock="Right" Width="60"/>
            <Button Content="目录" DockPanel.Dock="Right" Width="60"/>
            <TextBox Text="{Binding IndexesPath}"/>
        </DockPanel>
        <DockPanel></DockPanel>
    </DockPanel>
</Window>
